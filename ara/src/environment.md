# Environment

The published Capsule Docker images remain authoritative. The current completed author-default runs use isolated native Windows compatibility environments with the exact declared Python dependency versions; the checkpoint resume script performs full artifact-hash and test gates before execution.
