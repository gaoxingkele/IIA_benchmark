P0 Paper-Exact harness, frozen 2026-08-30.

run_1 = CASIM capsule v1 (author default is five-fold closed-set, not the paper open-set sweep)
run_2 = ConE-AFC capsule v2 (author default is five folds at alpha=.05, ncal/class=22)
run_3 = BiP-AFC capsule v3 (author code overlaps CP-calibration and RF-training indices)

The existing TEP/NPP/FCC Wave-2 report remains the immutable transfer_result lane.
This directory records author-capsule runs and must not relabel capsule-default subsets as complete P3 closure.

Commands:
  python scripts/paper_exact.py check --require-local --full-hash
  python scripts/paper_exact.py run-author --paper-id faulwasser2024_casim
  python scripts/paper_exact.py summarize --paper-id faulwasser2024_casim

Full ConE paper grid (wrapper only; official model source remains unchanged):
  python experiments/paper_harness/p0_paper_exact/paper_grid.py prepare-cone
  python experiments/paper_harness/p0_paper_exact/paper_grid.py run-cone --workers 4

The paper-grid runner reuses a read-only NumPy cache and checkpoints every split.
It evaluates 10x5 folds, all 51 prefixes, alpha .01/.05/.10, calibration sizes
22/102/2491, and additionally records singleton/empty rates omitted by the paper.

Full CASIM open-set reconstruction (wrapper adds the omitted 14-class loop;
the published get_train_test(..., open_set=True) splitter remains unchanged):
  python experiments/paper_harness/p0_paper_exact/paper_grid.py prepare-casim
  python experiments/paper_harness/p0_paper_exact/paper_grid.py run-casim --workers 4 --repetitions 1

For each held-out class, the wrapper relabels that class as -1 before the
published five-fold splitter. Thus every unknown sample appears in exactly one
test fold, rather than being duplicated across all five folds.

Paused checkpoint (2026-08-30 14:32 Asia/Shanghai):
  powershell -ExecutionPolicy Bypass -File scripts/resume_p0_checkpoint.ps1 -Target all

See checkpoint_2026-08-30.json for hashes and per-experiment resume semantics.
