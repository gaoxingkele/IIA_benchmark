# Concepts

- Benchmark role: SOTA early/open-set alarm flood classification
- Method summary: CASIM transforms alarm sequences with randomized convolutional kernels, pools the responses, and uses a ridge ensemble plus local outlier probability for open-set decisions.
- Access class: `open_access`
