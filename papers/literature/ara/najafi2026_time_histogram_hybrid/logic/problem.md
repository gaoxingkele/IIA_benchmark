# Problem

This source is included to support **SOTA early alarm-flood classification without chattering preprocessing**. The exact input schema,
prediction target, split unit, and metrics must be frozen in an experiment config
before the method can be called reproduced.
